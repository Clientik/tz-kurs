 computed: {
    filteredCurs() {
      return this.cb.filter((kurs) => {
        if (kurs.value >= this.sortdate.startdate && kurs.value <= this.sortdate.enddate) {
          return true;
        }
        return false;
      });
    },
  },
  watch: {
    filteredCurs() {
      if (this.filteredCurs.length < 7) {
        this.modevisor = 0;
      } else {
        this.modevisor = 1;
      }
    },
  },

.replace(/(\d*)-(\d*)-(\d*)/, '$3-$2-$1')